javascript:(function(){'use strict';function forceSelect(el){el.style.webkitUserSelect='text';el.style.mozUserSelect='text';el.style.msUserSelect='text';el.style.userSelect='text';el.style.webkitTouchCallout='text';el.style.pointerEvents='auto';if(el.children)Array.from(el.children).forEach(forceSelect);}function removeCSSRules(){let sheets=document.styleSheets;for(let sheet of sheets){try{for(let i=sheet.cssRules.length-1;i>=0;i--){let rule=sheet.cssRules[i];if(rule.cssText.includes('user-select')||rule.cssText.includes('pointer-events')||rule.cssText.includes('contextmenu')){sheet.deleteRule(i);}}}catch(e){}}}function forceEnableCopyPaste(e){e.stopImmediatePropagation();return true;}['copy','paste'].forEach(event=>{document.addEventListener(event,forceEnableCopyPaste,true);});let originalPrevent=Event.prototype.preventDefault;Event.prototype.preventDefault=function(){if(this.type==='contextmenu'||this.type==='selectstart'||this.type==='mousedown'||this.type==='copy'||this.type==='paste'){return;}originalPrevent.call(this);};let originalStop=Event.prototype.stopPropagation;Event.prototype.stopPropagation=function(){if(this.type==='contextmenu'||this.type==='selectstart'||this.type==='copy'||this.type==='paste'){return;}originalStop.call(this);};forceSelect(document.body);removeCSSRules();if(typeof jQuery!=='undefined'){jQuery.fn.on=function(types,selector,data,fn){if(types.includes('contextmenu')||types.includes('selectstart')||types.includes('copy')||types.includes('paste'))return this;return jQuery.fn.on.call(this,types,selector,data,fn);};jQuery('body *').off('contextmenu selectstart mousedown copy paste');}setInterval(()=>{forceSelect(document.body);removeCSSRules();['copy','paste'].forEach(event=>{document.addEventListener(event,forceEnableCopyPaste,true);});},200);console.log('Супер-байпас с копированием запущен! Проверь F12 > Console.');alert('.');})();